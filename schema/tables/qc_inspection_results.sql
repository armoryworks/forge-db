CREATE TABLE public.qc_inspection_results (
    id integer NOT NULL,
    inspection_id integer NOT NULL,
    checklist_item_id integer,
    description character varying(200) NOT NULL,
    passed boolean NOT NULL,
    measured_value character varying(200),
    notes character varying(500)
);

ALTER TABLE public.qc_inspection_results ALTER COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
    SEQUENCE NAME public.qc_inspection_results_id_seq
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
);

ALTER TABLE ONLY public.qc_inspection_results
    ADD CONSTRAINT pk_qc_inspection_results PRIMARY KEY (id);

ALTER TABLE ONLY public.qc_inspection_results
    ADD CONSTRAINT fk_qc_inspection_results_qc_checklist_items_checklist_item_id FOREIGN KEY (checklist_item_id) REFERENCES public.qc_checklist_items(id) ON DELETE RESTRICT;

ALTER TABLE ONLY public.qc_inspection_results
    ADD CONSTRAINT fk_qc_inspection_results_qc_inspections_inspection_id FOREIGN KEY (inspection_id) REFERENCES public.qc_inspections(id) ON DELETE CASCADE;
