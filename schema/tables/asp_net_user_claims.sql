CREATE TABLE public.asp_net_user_claims (
    id integer NOT NULL,
    user_id integer NOT NULL,
    claim_type text,
    claim_value text
);

ALTER TABLE public.asp_net_user_claims ALTER COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
    SEQUENCE NAME public.asp_net_user_claims_id_seq
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
);

ALTER TABLE ONLY public.asp_net_user_claims
    ADD CONSTRAINT pk_asp_net_user_claims PRIMARY KEY (id);

ALTER TABLE ONLY public.asp_net_user_claims
    ADD CONSTRAINT fk_asp_net_user_claims__asp_net_users_user_id FOREIGN KEY (user_id) REFERENCES public.asp_net_users(id) ON DELETE CASCADE;
