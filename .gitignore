# .NET build output
bin/
obj/
*.user

# Local scratch artifacts — the canonical pg_dump seed is consumed INTO schema/, never committed.
/baseline.schema.sql
*.local.sql

# Editor
.vs/
.idea/

# apply receipts are local OUTPUT (beta: no prod audit trail — revisit when in prod)
history/*.sql
